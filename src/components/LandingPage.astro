---
import Button from './ui/Button.astro';
import ElevatorPitch from './sections/ElevatorPitch.astro';
import KillerFeatures from './sections/KillerFeatures.astro';
import Comparison from './sections/Comparison.astro';
import UseCases from './sections/UseCases.astro';
import QuickStart from './sections/QuickStart.astro';
---

<main class="landing-main">
    <div class="bg-shape"></div>

    <div class="hero-section">
        <h1 class="hero-title">
            Planifica y ejecuta tareas con <br />
            <span class="highlight-brick">Precisión Agéntica</span>
        </h1>

        <p class="hero-subtitle">
            La documentación oficial de <strong class="text-ink">Architect</strong>. Herramienta CLI
            open source que orquesta agentes de IA para escribir, revisar, y corregir código
            automáticamente. <strong class="text-ink">Headless-first</strong>: diseñada para CI/CD,
            pipelines, y automatización. Multi-modelo y con guardrails de seguridad integrados.
        </p>

        <!-- Code Block — Real Human Logs -->
        <div class="code-figure">
            <div class="code-figure-header">
                <span class="code-figure-title">TERMINAL — HUMAN LOG OUTPUT</span>
                <div class="code-figure-dot"></div>
            </div>
            <div class="code-figure-content">
                <div><span class="prompt font-bold">$ </span><span class="font-bold">architect run "Refactoriza el módulo auth del proyecto"</span></div>
                <div class="log-spacer"></div>
                <div class="log-step"><span class="log-icon">&#x1F504;</span> Paso 1 &#x2192; Llamada al LLM (5 mensajes)</div>
                <div class="log-detail"><span class="log-check">&#x2713;</span> LLM respondió con 2 tool calls</div>
                <div class="log-spacer-sm"></div>
                <div class="log-tool"><span class="log-icon">&#x1F527;</span> read_file &#x2192; src/main.py</div>
                <div class="log-result"><span class="log-check">&#x2713;</span> OK</div>
                <div class="log-spacer-sm"></div>
                <div class="log-tool"><span class="log-icon">&#x1F527;</span> edit_file &#x2192; src/main.py (3&#x2192;5 líneas)</div>
                <div class="log-result"><span class="log-check">&#x2713;</span> OK</div>
                <div class="log-result"><span class="log-icon-sm">&#x1F50D;</span> Hook python-lint: <span class="log-check">&#x2713;</span></div>
                <div class="log-spacer"></div>
                <div class="log-step"><span class="log-icon">&#x1F504;</span> Paso 2 &#x2192; Llamada al LLM (9 mensajes)</div>
                <div class="log-detail"><span class="log-check">&#x2713;</span> LLM respondió con texto final</div>
                <div class="log-spacer"></div>
                <div class="log-done"><span class="log-icon">&#x2705;</span> Agente completado (2 pasos)</div>
                <div class="log-meta">Razón: LLM decidió que terminó</div>
                <div class="log-meta">Coste: <span class="text-arch-brick">$0.0042</span> <span class="cursor"></span></div>
            </div>
        </div>

        <div class="cta-group">
            <Button href={`${import.meta.env.BASE_URL}docs/`}>VER DOCS</Button>
            <Button variant="secondary" href="https://github.com/Diego303/architect-cli" target="_blank">GITHUB</Button>
        </div>
    </div>

    <!-- New marketing sections -->
    <ElevatorPitch />
    <KillerFeatures />
    <Comparison />
    <UseCases />
    <QuickStart />

</main>

<style>
    .landing-main {
        position: relative;
        padding-bottom: 5rem;
        overflow-x: hidden;
    }

    .bg-shape {
        position: absolute;
        top: -100px;
        right: -5%;
        width: 800px;
        height: 800px;
        background-color: var(--arch-shape);
        border: 1px solid var(--arch-line);
        border-radius: 50% 50% 0 0;
        z-index: -1;
        opacity: 0.6;
        pointer-events: none;
    }

    .hero-section {
        display: flex;
        flex-direction: column;
        align-items: center;
        text-align: center;
        padding: 5rem 1.5rem;
        max-width: 80rem;
        margin: 0 auto;
    }

    .hero-title {
        font-family: var(--font-heading);
        font-weight: 800;
        font-size: 3rem;
        line-height: 1.1;
        color: var(--arch-ink);
        margin-bottom: 1.5rem;
    }

    @media (min-width: 768px) {
        .hero-title {
            font-size: 4.5rem;
        }
    }

    .highlight-brick {
        position: relative;
        display: inline-block;
        color: var(--arch-brick);
    }

    .highlight-brick::after {
        content: '';
        position: absolute;
        bottom: 8px;
        left: 0;
        width: 100%;
        height: 12px;
        background-color: var(--arch-brick-transparent);
        z-index: -1;
        transform: skewX(-15deg);
    }

    .hero-subtitle {
        font-size: 1.125rem;
        color: var(--arch-ink-light);
        max-width: 42rem;
        margin-bottom: 3rem;
        line-height: 1.6;
    }

    @media (min-width: 768px) {
        .hero-subtitle {
            font-size: 1.25rem;
        }
    }

    .text-ink { color: var(--arch-ink); }

    /* Code Figure */
    .code-figure {
        width: 100%;
        max-width: 48rem;
        background-color: var(--arch-surface);
        border: 2px solid var(--arch-ink);
        box-shadow: var(--shadow-draft);
        text-align: left;
        margin-bottom: 4rem;
        overflow: hidden;
    }

    .code-figure-header {
        background-color: var(--arch-paper);
        border-bottom: 2px solid var(--arch-ink);
        padding: 0.75rem 1.5rem;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .code-figure-title {
        font-family: var(--font-mono);
        color: var(--arch-ink);
        font-size: 0.75rem;
        font-weight: 700;
        letter-spacing: 0.1em;
        text-transform: uppercase;
    }

    .code-figure-dot {
        width: 0.5rem;
        height: 0.5rem;
        border-radius: 50%;
        background-color: var(--arch-ink);
        opacity: 0.2;
    }

    .code-figure-content {
        padding: 1.5rem;
        font-family: var(--font-mono);
        font-size: 0.875rem;
        color: var(--arch-ink);
        line-height: 1.7;
        overflow-x: auto;
    }

    .prompt { color: var(--arch-ink-light); user-select: none; }
    .cursor { display: inline-block; width: 0.5rem; height: 1rem; background-color: var(--arch-brick); vertical-align: middle; animation: blink 1s infinite; margin-left: 0.25rem; }

    @keyframes blink { 50% { opacity: 0; } }

    /* Human Log Styles */
    .log-spacer { height: 0.75rem; }
    .log-spacer-sm { height: 0.25rem; }

    .log-step {
        color: var(--arch-ink);
        font-weight: 600;
    }

    .log-detail {
        padding-left: 1.5rem;
        color: var(--arch-ink-light);
    }

    .log-tool {
        padding-left: 1.5rem;
        color: var(--arch-ink);
    }

    .log-result {
        padding-left: 3rem;
        color: var(--arch-ink-light);
    }

    .log-done {
        color: var(--arch-ink);
        font-weight: 700;
    }

    .log-meta {
        padding-left: 1.5rem;
        color: var(--arch-ink-light);
        font-size: 0.8125rem;
    }

    .log-icon {
        margin-right: 0.375rem;
    }

    .log-icon-sm {
        margin-right: 0.25rem;
        font-size: 0.75rem;
    }

    .log-check {
        color: var(--arch-ink);
        font-weight: 700;
        margin-right: 0.25rem;
    }

    /* CTA */
    .cta-group {
        display: flex;
        flex-direction: column;
        gap: 1.5rem;
        width: 100%;
        max-width: 30rem;
    }

    @media (min-width: 640px) {
        .cta-group {
            flex-direction: row;
            justify-content: center;
        }
    }

    .font-bold { font-weight: 700; }
</style>
